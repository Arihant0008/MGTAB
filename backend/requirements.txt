# MGTAB Bot Detector — Backend Dependencies

fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0

# Utilities
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Upstash Redis — serverless cache for prediction results
upstash-redis>=1.1.0


# Scweet — Twitter/X scraping via GraphQL (no official API key needed)
Scweet>=5.0

# Firebase Admin SDK — server-side ID-token verification
firebase-admin>=6.4.0
